=== "Example"

    ```dax
    DaxLib.SVG.Viz.Pill(
        "Active",           // txt
        "#EC008C",        // color
        120,                // width
        28                  // height
    )
    // Returns an SVG data URI with a pill-shaped badge
    // Set as a measure with DataCategory = "ImageUrl"
    ```
