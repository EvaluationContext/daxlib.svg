---
icon: material/axis-arrow
---

# Axes

Axis layout, rendering, and positioning functions. These handle tick calculation, label sizing, axis line rendering, and coordinate mapping for chart axes. Used internally by `DaxLib.SVG.Compound.*` when `showAxis` is enabled, and available for custom axis implementations.
